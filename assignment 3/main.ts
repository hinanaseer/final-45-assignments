//storing the famous quote and its author

let quote:string = "Nothing is impodssible because impossible itself I M POSSIBLE"

let author:string = "hina Naseer";

//printing the message

console.log(`"${quote}" - ${author}`);